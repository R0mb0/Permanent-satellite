using SatellitePermanente.Database;

/*this static class is usefull for passing to debug monitor the extreme values calculated*/
static class GrayMapValues 
{ 
public static MaxCoordinates coordinates { get; set; }
}
